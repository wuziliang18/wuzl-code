package org.wuzl.test.algorithm;

import java.util.LinkedList;
import java.util.List;

/**
 * 组合总和 给定一个无重复元素的数组 candidates 和一个目标数 target ，找出 candidates 中所有可以使数字和为 target 的组合。
 * 
 * candidates 中的数字可以无限制重复被选取。
 * 
 * 说明：
 * 
 * 所有数字（包括 target）都是正整数。 解集不能包含重复的组合。 
 * 
 * 链接：https://leetcode-cn.com/problems/combination-sum 1 <= candidates.length <= 30 1 <= candidates[i] <= 200 candidate
 * 中的每个元素都是独一无二的。 1 <= target <= 500
 * 
 * @author ziliang.wu
 *
 */
public class CombinationSum {
    public List<List<Integer>> combinationSum(int[] candidates, int target) {
        LinkedList<Integer>[] array = new LinkedList[target + 1];
        return null;
    }
}
